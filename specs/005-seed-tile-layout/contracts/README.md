# Contracts: `005-seed-tile-layout`

This feature’s **external** contracts are **UI behavior** (Briefcase field + navigation) and **pure function** semantics for deterministic dealing.

| Document | Purpose |
|----------|---------|
| [`seed-deal.contract.md`](./seed-deal.contract.md) | Inputs/outputs and invariants for seed → shuffle pipeline (Vitest golden vectors). |

No HTTP APIs or storage schema changes for this feature.
