# Scripts

## `ingest-assets.mjs` (placeholder)

Future **one-time** pipeline to download CS2-themed images from catalog data (see [ByMykel/CSGO-API](https://github.com/ByMykel/CSGO-API)) into `public/` (or similar) so the **runtime game never calls the API**.

Implementation will be added with the asset-ingest feature; this stub documents the intent for reviewers.
